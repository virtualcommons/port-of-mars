export enum ChatMarsLogView {
  Chat,
  MarsLog,
  Split,
}

export enum HUDLeftView {
  OtherPlayers,
  Inventory,
  Accomplishments,
}

export enum HUDRightView {
  PhaseInformation,
  ActiveEvents,
}
